# Только на сервере: /etc/balans/migration.env, права 600.
# DATABASE_URL — та же runtime-роль, что в runtime.env.
DATABASE_URL=postgresql://balans_app:CHANGE_ME@host.docker.internal:5432/balans
MIGRATION_DATABASE_URL=postgresql://balans_owner:CHANGE_ME@host.docker.internal:5432/balans
